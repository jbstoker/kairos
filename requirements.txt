pytest>=7.0
flask>=2.0

# Kairos Time (KST) — the celestial engine (optional; lazy-loaded).
#   pip install skyfield
# (Meeus and the SunCalc port are always available; no install required.)
skyfield>=1.42
numpy>=1.24.0
jplephem>=2.21
